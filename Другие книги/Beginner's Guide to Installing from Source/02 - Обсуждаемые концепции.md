
- **Разработка и дистрибуция:** независимые разработчики ПО, поддержка нескольких ОС, релизы (архивы), контроль версий

- **Дистрибутивы**: бинарные пакеты и менеджеры пакетов

- **Загрузка**: `http`, `ftp`, `wget`, контрольные суммы и подписи

- **Архивные файлы**: `tar`, `zip`, `gzip`

- **Общие файлы:** README и INSTALL; необходимые зависимости

- **Применение исправлений** с помощью `sed/awk/patch`

- **Сборка и установка:** `configure`, `make`, `cmake`, `perl` и `python`

- Сборка и установка документации

- **Настройки компилятора** и очистка (stripping); обработка ошибок

- Послеустановочная настройка


---
[[Навигация по книге Beginner's Guide to Installing from Source]]


### Зачем вам это нужно?

Это оглавление показывает **полный цикл работы с исходным кодом** — от загрузки до пост-установки. В LFS вы пройдёте через все эти этапы для каждого пакета.